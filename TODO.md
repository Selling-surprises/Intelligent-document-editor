# 任务: 悬浮目录颜色和透明度与遮罩层一致

## 计划
- [x] 修改悬浮目录的背景颜色透明度
  - [x] 使用settings.opacity / 100作为透明度
  - [x] 与遮罩层的透明度保持一致
- [x] 运行lint检查确保代码质量

## 注意事项
- 悬浮目录的背景颜色应该与遮罩层使用相同的透明度
- 遮罩层使用settings.opacity / 100作为透明度值
- 黑色遮罩使用rgba(0, 0, 0, opacity)
- 白色遮罩使用rgba(255, 255, 255, opacity)
- 所有修改已通过lint检查

## 完成情况
✅ 修改悬浮目录背景颜色透明度:
  - 在Editor.tsx第1363行修改悬浮目录的background属性
  - 黑色遮罩模式：从rgba(0, 0, 0, 0.95)改为rgba(0, 0, 0, ${settings.opacity / 100})
  - 白色遮罩模式：从rgba(255, 255, 255, 0.98)改为rgba(255, 255, 255, ${settings.opacity / 100})
  - 悬浮目录的透明度现在与遮罩层完全一致
  - 用户调整遮罩透明度时，悬浮目录会同步变化
